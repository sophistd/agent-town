import { describe, expect, it } from "vitest";

describe("project scaffold", () => {
  it("keeps the test runner available for later sessions", () => {
    expect("AgentEvent").toBe("AgentEvent");
  });
});
