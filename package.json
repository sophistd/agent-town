{
  "name": "agent-town",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "smallville:scheduler": "node scripts/smallville-autonomous-scheduler.mjs",
    "smallville:runtime-stream": "node scripts/smallville-runtime-stream.mjs",
    "world-memory:server": "node scripts/world-memory-server.mjs",
    "world-memory:provider-loop": "node scripts/world-memory-provider-loop.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "test": "vitest run",
    "build": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json && vite build"
  },
  "dependencies": {
    "phaser": "^4.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
